#include <bits/stdc++.h>
using namespace std;

int main()
{
    string a, b;
    cin >> a >> b;
    string c = "", w;
    if (a == "0")
    {
        c = "0";
    }
    if (b == "0")
    {
        c = "0";
    }
    int sign = ((a[0] - '0') ^ (b[0] - '0'));
    string X;
    cout << sign;//TODO: NOT REQUIRED! But not finished
    return 0;
}